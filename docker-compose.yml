version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www/html
    container_name: app

  nginx:
    build:
      context: /home/s_ishihara/projects/circle_of_creation/develop
      dockerfile: /home/s_ishihara/projects/circle_of_creation/develop/docker/nginx/Dockerfile
    ports:
      - "8001:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    container_name: nginx

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laravel
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3301:3306"
    volumes:
      - dbdata:/var/lib/mysql
    container_name: mysql

volumes:
  dbdata:
